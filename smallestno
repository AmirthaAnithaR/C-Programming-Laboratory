#include<stdio.h>
int main(){
    int a,b;
    scanf("%d %d",&a,&b);
    if (a>b){
        printf("%d is smaller than %d",b,a);
    }
    else if(b>a){
        printf("%d is smaller than %d",a,b);
    }
    else{
        printf("they are equal");
    }
}

